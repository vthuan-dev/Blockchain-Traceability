/**
 * Reduces the values yielded by an (async) iterable
 */
export default async function reduce(source, func, init) {
    for await (const val of source) {
        init = func(init, val);
    }
    return init;
}
//# sourceMappingURL=index.js.map